import assert from 'assert';
import reduxReducerMeld from '../lib';

describe('redux-reducer-meld', function () {
  it('should have unit test!', function () {
    // @todo test undefined previousState with primitive initialState
    // @todo test undefined previousState with object initialState

    // @todo test defined previousState with primitive initialState

    // @todo test partial previousState with object initialState
    // @todo test complete previousState with object initialState
  });
});
